SECRET_KEY=change-me-to-a-long-random-string
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Email (used for the "welcome / account created" notification)
# Leave EMAIL_BACKEND as the console backend to just print emails in the
# terminal while developing. Switch to SMTP to send real emails.
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=chigozieallanie@gmail.com
EMAIL_HOST_PASSWORD=rqmlsdwzbgjynzic
DEFAULT_FROM_EMAIL=VeeFinds <chigozieallanie@gmail.com>

# SMS (used for the phone activation code)
# SMS_BACKEND=console just prints the code to your terminal - good for dev.
# Set SMS_BACKEND=twilio and fill in the 3 values below to send real texts.
SMS_BACKEND=console
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
